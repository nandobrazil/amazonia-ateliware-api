import { AuthRequest } from '../../auth/models/AuthRequest';

export const mockAuthRequest = {
  user: {
    id: 1,
    username: 'test',
    name: 'test',
    password: undefined,
  },
} as AuthRequest;
